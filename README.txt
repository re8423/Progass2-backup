To install necessary packagages: npm install

To start the server: npm start

When uploading images, images can only be .png files. They will be stored under client/images

All other data (paths, name, difficulty, completed names) will be stored in Data.json (However if the server restarts the data will reset as well).

index.js consists of client side javascript

app.js consists of all of the post/get requests